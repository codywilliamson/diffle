// what's-new highlights for the current release. auto-shown once per version the reviewer
// hasn't seen; the seen version persists server-side (state.json) via /api/state.
export const WHATS_NEW = {
  version: "0.16.0",
  highlights: [
    "Rebuilt in a dark-first indigo look — the new D5 design system.",
    "A faster Svelte client: side-by-side view, line wrapping, and a sanitized Markdown preview.",
    "The same review workflow — inline comments, drag-select ranges, and structured feedback for your agent.",
  ],
};
